<!DOCTYPE html>
<html lang="en" xmlns:th="https://www.thymeleaf.org">
<head>
<meta charset="ISO-8859-1">
<title>Insert title here</title>
<style>
#super{display:flex;background-color: lightgrey}
#super div{margin:10px}
body{background:url("../img/bg.jpg"); background-repeat:no-repeat;background-position:inherit;background-size:cover; margin:0px;padding:0px}
*{font-size:15px;font-family: cursive; box-sizing: border-box;text-decoration: none}
.bg-text {
color:rgb(255, 255, 255);
font-weight: bold;
border: 3px solid #f1f1f1;
border-radius:25px;
position: absolute;
top: 48%;
left: 50%;
transform: translate(-50%, -50%);
z-index: 2;
height:45%;
width: 35%;
padding: 10px;
text-align: center;
background-color:lightgrey;
}
.bg-text div {margin:15px;}

</style>
</head>
<body>

<div id="super">
<div>
<h1>Currency Converter</h1>
</div>
</div>
<div class="bg-text">
	
	<form action="#" th:action="@{/convert}" th:object="${user}" >
	 <div class="mb-3">
	<h2>Indian Currency Converter</h2>
    <label for="currency1" class="form-label">Amount</label>
    <input type="number" th:field="*{currency1}" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
    <div class="mb-3">
    <label class="form-label">Convert From</label>
   <select th:field="*{from}">
   <option th:value="dollar" th:text="dollar"></option>
   <option th:value="dollar" th:text="dollar"></option>
   <option th:value="pound" th:text="pound"></option>
   <option th:value="rubel" th:text="rubel"></option>
    <option th:value="newshekel" th:text="newshekel"></option>
   <option th:value=" swissfranc" th:text="swissfranc"></option>
   <option th:value="rupee" th:text="rupee"></option>
   </select>
  </div>
    <div class="mb-3">
    <label class="form-label">Convert To</label>
   <select th:field="*{currencyname}">
   <option th:value="dollar" th:text="dollar"></option>
   <option th:value="pound" th:text="pound"></option>
   <option th:value="rubel" th:text="rubel"></option>
    <option th:value="newshekel" th:text="newshekel"></option>
   <option th:value=" swissfranc" th:text="swissfranc"></option>
    <option th:value="rupee" th:text="rupee"></option>
   </select>
  </div>
  <button type="submit" class="btn btn-primary">convert</button>
  </div>
</form>
<p th:text="${result}"></p>
</div>

</body>
</html>